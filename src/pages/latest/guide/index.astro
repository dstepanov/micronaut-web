---
import { withBasePath } from "@/lib/base-path";
import {
  clientRedirectDocument,
  routeCompatibilityEntry,
} from "@/lib/route-compatibility";

const compatibility = routeCompatibilityEntry("docs-core-latest-guide");
const destination = withBasePath(compatibility.previewDestinationPath);
---

<Fragment
  set:html={clientRedirectDocument(destination, "Micronaut Core docs")}
/>
